{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "design-system",
  "root": "packages/design-system",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/design-system build" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/design-system typecheck" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/design-system lint" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/design-system test" } }
  }
}