{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "platform-core",
  "root": "packages/platform-core",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/platform-core build" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/platform-core typecheck" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/platform-core lint" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "pnpm -C packages/platform-core test" } }
  }
}